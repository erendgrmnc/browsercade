# WebSocket URL of the multiplayer server.
# Defaults to the deployed server if unset (see src/config/server.ts).
# For local dev against a local Go server, use:
#   VITE_SERVER_URL=ws://localhost:8080/ws
VITE_SERVER_URL=wss://chess3d-server.onrender.com/ws
